# Environment template — NEVER put real values here or in any committed file.
# Real secrets live in ~/.hermes/.env on the machine (Layer 23 secret firewall).

SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_ANON_KEY=***
# Service role + PAT stay in ~/.hermes/.env only — never in this repo.
NETLIFY_SITE_NAME=kaszael-chit-chat
