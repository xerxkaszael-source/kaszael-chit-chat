# Build artifacts
build/
deploy-fix.zip
deploy.zip

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env (sandbox only — env lives in ~/.hermes/.env)
.env
.env.local

# Backups (per workspace convention)
.bak/
